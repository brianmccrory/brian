#+title: Indexes subdomain
#+date: 2025-09-10
#+draft: true
#+tags[]: notags

* September 2025: Move resources (unified index, etc) to gitpages at docs.jazzofjapan.com

/Note: =docs.jazzofjapan.com= was later changed to =about.jazzofjapan.com=./

- Implemented: 2025-09-10
- First post under new system: 2025-09-13 (https://www.jazzofjapan.com/p/takako-yamada-trio-live-at-the-moment)
  - finished wip branch, merged to main
  - ran ~publish-hugo.sh~ to update =brianmccrory.github.io= repo
  - commit/push triggers github actions
  - live docs site is automatically rebuilt

PRIMARY GOAL:
- when publishing new article to substack, avoid having to update unified index page

SECONDARY GOALS:
- remove index-type pages from substack
  - not rendered well (not prose text, long lists with small margins and lots of wrapping, no support for tables, etc)
  - did not fit within the basic three types of posts (albums, clubs, guides) so were abnormal/outliers which were regularly being updated
  - could trigger some checks or potential banning but substack by resembling “link farm” or seo-abuse type pages
- format index page / pages as I like
- do the same for categories pages
- consider the same for audio excerpts

PROCESS TO TRIGGER AUTOMATED HUGO PUBLISH:
1. in repo =writing=:
   1. publish new album, club, etc.
   2. run ~./scripts/publish_hugo.sh~
   3. (TODO reconsider) git commit files in =generated/= as well as others
2. in repo =brianmccrory.github.io=:
   1. git diff, git add, git commit, git push
   2. push will trigger action for github pages build

PROCESS TO EXTEND/TEST HUGO:
1. in repo =brianmccrory.github.io=:
   - ~hugo server~
   - http://localhost:1313

HUGO: https://gohugo.io

TASKS COMPLETED:
- updated dns (namecheap) for subdomain
- updated github pages =brianmccrory.github.io= to add subdomain
- update build script =.github/workflows/hugo-gh-pages.yml=
- test github actions
- the code in =hugo-gh-pages= runs hugo, which builds the site (using the checked in markdown in =content/=), so the custom lisp code in =build-site-hugo.el= is no longer needed.
  - this was used for ‘manual builds’ (or custom actions) to modify the original jekyll build, which also pulled in content from another repo =brianmcc=, to keep the content separate in a private repo.
